let waterCreature = ["barracuda", "eel", "shark"];
document.getElementById("demo").innerHTML = waterCreature;

// We can simply add to the end of the array by doing using out push() method like so...
waterCreature.push("mermaid"); // This adds "mermaid" to the end of the arr waterCreature

// We can then call and return the updated array like so....
document.getElementById("demo1").innerHTML = waterCreature;
